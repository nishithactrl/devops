name: Simple CI Demo

on:
  push:
    branches: [ "main" ]

jobs:
  demo-job:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - run: ls

      - run: echo "GitHub Actions working!"
